# 🧙‍♂️ WinxClubSup Bot — v7 Final (GitHub + Railway)

Полный Telegram‑бот для управления профилем игроков, БМ, отсутствиями и аукционными очередями с Google Sheets.

## ✨ Ключевые возможности
- Единый профиль: ник, класс, БМ (+история прироста)
- Три темы в группе: **Инфо / Аукцион / Отсутствие** (привязки)
- Очереди аукциона по предметам (запись, выход, просмотр, отметка “забрал”)
- **Множественный выбор** предметов в меню аукциона и в команде `/очередь`
- Управление предметами прямо из Telegram (добавить/удалить/список)
- Роли и права (лидер + офицеры)
- Автоудаление: сообщение игрока — сразу, ответ бота — через 15 секунд
- Логи действий в листе “Лог” Google Sheets

---

## 💬 Полный список команд

### Игроки
- `/ник <имя>` — установить/сменить ник
- `/класс` — выбрать класс (кнопки)
- `/бм <число>` — обновить БМ (вносится в историю)
- `/профиль` — показать профиль
- `/топбм` — топ прироста БМ за 7 дней
- `/нет <дд.мм> <причина>` или `/отсутствие [дд.мм причина]` — отметить отсутствие

### Аукцион
- `/аук` — выбор нескольких предметов и запись в очередь (подтверждение кнопкой)
- `/очередь [предмет]` — если предмет указан — покажет очередь; если нет — откроет **клавиатуру выбора нескольких предметов**, затем выведет очереди и подпишет, кто запросил
- `/выйти [предмет]` — выйти из очереди по предмету или из всех
- `/забрал` — отметить полученные предметы; если был в очереди — переместиться в конец

### Для лидера и офицеров
- `/удалить <предмет> <ник>` — удалить игрока из очереди по предмету
- `/добавить_предмет <название>` — добавить новый столбец “Аукцион”
- `/удалить_предмет <название>` — удалить столбец (с очередями)
- `/список_предметов` — вывести текущие предметы
- Привязки тем: `/привязать_инфо`, `/привязать_аук`, `/привязать_отсутствие`, `/отвязать_все`
- `/help_master` — показать краткую памятку по командам

> В меню Telegram фиксированы латинские команды (BotCommand), но русские аналоги тоже работают.

---

## 📊 Структура Google Sheets
Листы: **Игроки**, **История БМ**, **Аукцион**, **Отсутствия**, **Лог**, **Аналитика**.  
В листе **Аукцион** первая строка — это названия предметов (столбцы).

---

## 🚀 Деплой через GitHub → Railway
1. Создай репозиторий `winxclubsup_bot` и залей файлы архива.
2. Railway → New Project → Deploy from GitHub Repo.
3. Добавь переменные окружения:
   - `BOT_TOKEN`
   - `GSHEET_ID`
   - `LEADER_ID` (например, `@yakovlef`)
   - `GOOGLE_CREDENTIALS` (JSON-ключ сервисного аккаунта **в одну строку**)
   - `STARTUP_ANNOUNCE=1` (по желанию)
4. Запусти Deploy. В группе привяжи темы:
   - `/привязать_инфо`, `/привязать_аук`, `/привязать_отсутствие`

---

## 🔐 Роли
- Лидер: `LEADER_ID`
- Офицеры (по умолчанию): `@Maffins89`, `@Gi_Di_Al`, `@oOMEMCH1KOo`, `@Ferbi55`, `@Ahaha_Ohoho`  
  (можно изменить в `main.py`, переменная `OFFICERS`)

---

## 🛠️ Тех. детали
- Python 3.11, aiogram 2.25.1, SQLite, gspread
- Автоудаление: сообщение игрока удаляется сразу, ответ бота — через 15 сек
- Все действия логируются в лист “Лог”
